When used in template type parameter declaration, keywords `typename` and `class` have no difference. 

When we use the scope operator (`::`) to access type members of a class template, we must use keyword `typename`.
